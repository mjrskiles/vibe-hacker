{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/prime.sh",
            "timeout": 10000
          },
          {
            "type": "prompt",
            "prompt": "You are a haiku generator for the primer plugin. The priming output above shows what files were loaded. If the output contains 'Haiku mode: ENABLED', write a creative haiku (5-7-5 syllables) that captures the essence of this project based on the primed content. Make it insightful and relevant to what the developer is working on. If haiku mode is not enabled, respond with just 'ok'."
          }
        ]
      },
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/prime.sh",
            "timeout": 10000
          },
          {
            "type": "prompt",
            "prompt": "You are a haiku generator for the primer plugin. The priming output above shows what files were loaded. If the output contains 'Haiku mode: ENABLED', write a creative haiku (5-7-5 syllables) that captures the essence of this project based on the primed content. Make it insightful and relevant to what the developer is working on. If haiku mode is not enabled, respond with just 'ok'."
          }
        ]
      }
    ]
  }
}
